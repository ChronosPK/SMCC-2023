1. Name (for the leaderboard):


2. SQL injection attempts to interact with the ... <br>
`database`


3. OPTIONS, HEAD and GET have a thing in common: all of them are HTTP ... <br>
`verbs, methods, request methods`


4. User input is never ... <br>
`trusted, reliable`


5. Attack based on interface that attempts to trick a user into clicking on a hidden link: ... <br>
`clickjacking`


6. You can read arbitrary files from a server with the help of a badly configured service and a parameter that accepts input: ... <br>
`directory traversal, path traversal, arbitrary file read, LFI`


7. You can execute OS commands on a server: ... <br>
`command injection, command execution, OS command injection`


8. You can upload unverified files to a server: ... vulnerability <br>
`file upload, unrestricted file upload`


9. What is the term used to describe a cyber attack method in which an attacker repeatedly tries different password or encryption keys combinations until the correct one is found? <br>
`bruteforce, brute force, bruteforce attack, brute force attack`


10. PHP, Ruby and Java could have problems with the conversion of data structures: ... (vulnerability) <br>
`insecure deserialization, insecure serialization, serialization, deserialization`

