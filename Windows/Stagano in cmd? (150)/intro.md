## Intro:

Did you know you can hide an archive inside an image, all from the command line? <br>
Let's pretend you want to hide `passwords.rar` inside the image `cars.png`. The new image will be called `cars_background.png`. <br>

Flag format: `command`
