## Hint:

This is what you'll need to use:
<br>
- `curl` to request data

- In order to bruteforce (also known as FUZZ) the password, you firstly need a wordlist (`rockyou.txt` is a standard) <br>
	- You can follow the bruteforce process in 2 ways:
		- using an online tool, usch as `wfuzz` or `ffuf`
		- using a "one-liner", such as a loop function that replaces a variable with the elements of a list

- `netstat` to see internal ports listening, Ethernet statistics, the IP routing table, IPv4 statistics 
<br>
Everything I listed above can be found on the internet as well easily, so don't wait too much, this ain't that hard!
